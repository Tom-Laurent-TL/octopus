# Example Environment Configuration
# Copy this file to .env and update with your actual values

# Master API Key - Set this after running bootstrap
MASTER_API_KEY=default-api-key

# Database Configuration
# ---
# For LOCAL DEVELOPMENT (running with `uv run fastapi dev`):
DATABASE_URL=sqlite:///./data/chat_conversations.db

# For DOCKER (running with `docker-compose up`):
# DATABASE_URL=sqlite:////app/data/chat_conversations.db
# Note: Docker Compose automatically overrides this, so you can keep the local path

# Database path explanation:
# - sqlite:///./file.db       = Relative path (3 slashes for relative)
# - sqlite:////absolute/path  = Absolute path (4 slashes for absolute)
